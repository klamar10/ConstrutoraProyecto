<p id="notice"><%= notice %></p>

<h1>Departamentos</h1>

<table>
  <thead>
    <tr>
      <th>Piso</th>
      <th>Caracteristicas</th>
      <th>Area</th>
      <th>Tipo</th>
      <th>Precio</th>
      <th>Mantenis</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @departamentos.each do |departamento| %>
      <tr>
        <td><%= departamento.piso %></td>
        <td><%= departamento.caracteristicas %></td>
        <td><%= departamento.area %></td>
        <td><%= departamento.tipo %></td>
        <td><%= departamento.precio %></td>
        <td><%= departamento.mantenis %></td>
        <td><%= link_to 'Show', departamento %></td>
        <td><%= link_to 'Edit', edit_departamento_path(departamento) %></td>
        <td><%= link_to 'Destroy', departamento, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Departamento', new_departamento_path %>
